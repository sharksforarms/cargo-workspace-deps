[package]
name = "member1"
version = "0.1.0"
edition = "2021"

# Important comment about dependencies
[dependencies]
# This comment should be preserved
serde = { workspace = true }
anyhow = { workspace = true }
existing-dep = { workspace = true }
